import { counterReducers } from './model/slice/counterSlice';
import { CounterSchema } from './model/types/counterSchema';
import Counter from './ui/Counter';

export { Counter, CounterSchema, counterReducers };
